{
  "name": "webprime",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "vite",
    "build": "node build.js",
    "preview": "vite preview --port 8082 dist/",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "@teskevirtualsystem/jpak": "^3.0.0",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^8.0.16"
  }
}
